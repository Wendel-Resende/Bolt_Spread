{
  "name": "spread-analysis",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "pip3 install -r requirements.txt --user",
    "start": "python3 -m streamlit run src/main.py"
  }
}